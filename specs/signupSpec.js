describe("User Signup", function() {

    // var page = require("../pageObjects/signup.js");
    // var data = require('../lib/data.js').data;

    // beforeEach(function() {
    // 	isAngularSite(false);
    //     page.go("http://127.0.0.1:3000/users/sign_up");
    // });

    // it('should sign up to be a user', function () {
    //     page.enter_email(data.user.email);
    //     page.enter_password(data.user.password);
    //     page.enter_password_confirm(data.user.password);
    //     page.signup_button().click().then(function () {
    //         expect(page.getCurrentUrl()).toContain('users/edit');
    //     });
    // });

});
